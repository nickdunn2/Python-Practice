
def has_exact_length(flight_length, movie_lengths):
    current_movie = movie_lengths.pop()

    for movie_length in movie_lengths:
        print(movie_length)


test_one = has_exact_length(14, [7, 7]))
